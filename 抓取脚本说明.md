# football-dashboard 抓取脚本包

Termux 定时抓取链路所需的全部脚本(2026-08-30 打包自 football-dashboard@341d026)。

## 安装/使用

```bash
# 依赖(第三方库)
pip install -r requirements.txt
# 或按需: opencc scipy numpy

# 主调度: 抓取zqdc北单+HKJC → 分析 → 回填 → 推GitHub
./scripts/fetch_and_push.sh [日期, 默认今天]

# 北单看板(生成xlsx+index.html → 推 football-odds-api)
./scripts/beidan_cron.sh

# 单脚本: 香港马会全量抓取
python3 scripts/fetch_hkjc_all.py --date 2026-08-30 --parallel 5 --delay 0.15
```

注意: `titan007_utils.py` 是共享工具库(抓取/赔率/队名翻译), 各 fetch_*.py 均依赖它, 须同目录放置。

## 抓取脚本清单

| 脚本 | 用途 |
|---|---|
| fetch_zqdc.py | 北单/竞彩 zqdc 赛事抓取(--backfill 回填比分) |
| fetch_hkjc_all.py | 香港马会(HKJC)赔率全量抓取(--parallel/--delay) |
| fetch_analysis_data.py | titan007 analysis/detail 页完整统计抓取 |
| fetch_daily_xg.py | 每日 xG 历史趋势表抓取 |
| fetch_standings.py | 联赛积分榜抓取 |
| fetch_stats.py | 技术统计抓取(被 fetch_analysis_data/ai_analysis 依赖) |
| fetch_xg_data.py | xG 原始数据抓取 |
| prefetch_stats_cache.py | 统计缓存预抓 |
| backfill_1x2d_odds.py | 1x2d 初/终盘赔率回补 |
| backfill_ah.py | 亚盘回补 |
| backfill_ah_probs.py | 亚盘概率回补 |
| backfill_open_odds.py | 开盘价回补 |
| backfill_over_historical.py | 历史大小球回补 |
| backfill_poisson_probs.py | 泊松概率回补 |
| backfill_rankings_detail.py | 排名详情回补 |
| backfill_standings.py | 积分榜回补 |
| backfill_tech_stats.py | 技术统计回补 |
| backfill_xg.py | xG 回补 |
| titan007_utils.py | 核心工具库(fetch_url/赔率/队名翻译, 必须同目录) |
| ai_analysis.py | AI 分析(被 fetch_analysis_data 依赖; 需 scipy/numpy) |

## 调度脚本

| 脚本 | cron | 说明 |
|---|---|---|
| fetch_and_push.sh | 12:30 | 主链路: 抓取→回填→同步→分析→推送GitHub+ntfy |
| beidan_cron.sh | 13:00 | 北单看板: gen_500_dashboard → gen_beidan_html → 推 football-odds-api |

## 数据/DB

- 脚本写库: `football.db`(项目根, 由 rebuild_db.py 初始化)
- 抓取结果 JSON: `data/matches_YYYYMMDD.json`、`data/matches_hkjc_YYYYMMDD.json`(fetch_and_push.sh 按此文件名读)
- 需在项目根目录运行(脚本内用相对路径)

## 打包范围说明

仅含抓取/回填/调度链代码。未含: 分析类(away_value_picks/gen_ledger 等)、调试脚本(probe_*/tmp_*/bt_*)、数据、缓存、日志。
